# Implementation plan

1. Add explicit decision bucket contracts and deterministic orchestration entrypoint.
2. Enforce hard veto ordering (universe, ET windows, lockouts, freshness, risk guards).
3. Implement v1 feature/regime engine and Strategy A setup families.
4. Apply contract/execution/risk defaults for conservative paper launch.
5. Integrate OpenAI context + adversarial outputs with deterministic arbiter.
6. Add Strategy B recommend-only quota/payload discipline.
7. Persist decision snapshots and expose replay-focused endpoints/UI.
8. Harden tests and docs.
